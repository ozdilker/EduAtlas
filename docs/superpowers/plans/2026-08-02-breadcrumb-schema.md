# Breadcrumb Schema Implementation Plan

**Goal:** Exactly one BreadcrumbList on each indexable page via BreadcrumbSchemaBuilder + SchemaEngine.

- [x] BreadcrumbSchemaBuilder (CanonicalResolver item URLs)
- [x] Adapters + home coverage; last crumb includes path
- [x] Legacy helper delegates; tests + typecheck

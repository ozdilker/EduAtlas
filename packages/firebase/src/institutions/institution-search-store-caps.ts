/** Max docs a store exact-name query may return (GC 20, public 10). */
export const INSTITUTION_EXACT_NAME_STORE_CAP = 20;

/** Max docs a store keyword array-contains query may return (public 40). */
export const INSTITUTION_KEYWORD_STORE_CAP = 40;
